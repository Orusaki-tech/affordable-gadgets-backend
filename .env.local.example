# Local development overrides
# Copy to .env.local and adjust if needed. Values here override .env when running locally.
# .env.local is gitignored; use it so you never commit local-only settings.
#
# Do NOT copy secrets from .env (SECRET_KEY, API keys, passwords). Those stay in .env.
# Only set variables you want different for local runs.

# --- Required for local (different from production) ---
# Use development mode: no production settings (no HTTPS redirect, local CORS)
DJANGO_ENV=development

# Show debug pages and allow localhost in Django
DEBUG=True

# --- Optional overrides ---
# Use SQLite instead of Postgres (leave empty or commented to use DATABASE_URL from .env)
DATABASE_URL=postgresql://affordable:affordable@localhost:5432/affordable_gadgets

# Print emails to console instead of sending (no SMTP needed)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Only if you ever run with DJANGO_ENV=production locally:
SECURE_SSL_REDIRECT=false

# Use Pesapal sandbox from local so you don't hit production payments
PESAPAL_ENVIRONMENT=sandbox

# Base URL for email links (e.g. verification). Match your local e-commerce frontend port.
# FRONTEND_BASE_URL=http://localhost:3000

# Base URL for the admin frontend (if used in emails/links/redirects). Match your local admin app port.
# ADMIN_BASE_URL=http://localhost:3001



