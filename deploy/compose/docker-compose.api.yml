# Reference compose for API (production uses Ansible template on VMs).
services:
  web:
    image: "${API_IMAGE:-ag-api}:${IMAGE_TAG:-latest}"
    ports:
      - "8000:8000"
    env_file: api.env
    restart: unless-stopped
