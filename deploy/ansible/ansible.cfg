[defaults]
inventory = inventory/production
roles_path = roles
host_key_checking = False
stdout_callback = default

[vault]
# Encrypt secrets: ansible-vault encrypt secrets/production.vault.yml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
