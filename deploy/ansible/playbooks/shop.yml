---
- name: Deploy shop to platform MIG
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../group_vars/all/00_system.yml
    - ../group_vars/{{ env_name | default('staging') }}.yml
    - ../vars/generated_from_terraform.yml
  roles:
    - shop_compose
